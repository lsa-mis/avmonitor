<%= turbo_frame_tag dom_id(room) do  %>
  <h2 class="sr-only">room</h2>
  <div class="mt-2 mb-3 px-2 sm:px-0">
    <div>
      <div class="text-xs px-2 font-bold hover:underline">
        <%= link_to room.facility_id, room, data: { "turbo-frame": "_top" } %>
        
      </div>
      <div class="text-xs px-2 font-bold hover:underline">
        <%= room.building %>
      </div>

  </div>
  
<% end %>