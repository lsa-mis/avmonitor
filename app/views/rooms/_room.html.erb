<div id="<%= dom_id room %>">
  <div class="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-2 m-4">
    <div>
      <%= render 'systems', { room: room } %>
    </div>
    <div>
      <%= render 'audio', { room: room } %>
    </div>
  </div>
</div>
